// @ts-ignore: imported JS module without type declarations
// @ts-ignore: imported JS module without type declarations
import GRPreview from "./GR_Preview";

function App() {
  return <GRPreview />;
}

export default App;